class VehicleModel {
  int id;
  String name;
  double price;
  VehicleModel({
    required this.id,
    required this.name,
    required this.price,
  });

  List lists = [
    'Car',
    'Bus',
    'Truck',
  ];
}
